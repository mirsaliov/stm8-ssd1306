#ifndef _ASCII_FONT_H_
#define _ASCII_FONT_H_

/*
 * ASCII-шрифт 5x7
 *
 * Диапазон символов: 0x20 (' ') – 0x7E ('~')
 * Каждый символ представлен 5 байтами.
 * Один байт соответствует вертикальному столбцу из 8 пикселей.
 *
 * Шрифт используется только для текстового вывода.
 * Логика дисплея в этот файл НЕ входит.
 */
extern const unsigned char ascii_font_5x7[95][5];

#endif
